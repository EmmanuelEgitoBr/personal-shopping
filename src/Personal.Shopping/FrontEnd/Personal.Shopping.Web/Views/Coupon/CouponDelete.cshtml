@using Personal.Shopping.Web.Models.Coupon
@model CouponDto

<form asp-action="CouponDelete">
    <br />
    <div class="container border p-3">
        <h1 class="text-white text-center">Apagar cupom de desconto</h1>
        <input asp-for="CouponId" hidden />
        <hr />
        <div class="row">
            <div class="col-2">
                <label class="control-label pt-2" style="font-size:20px;">Código do cupom</label>
            </div>
            <div class="col-10 pb-3">
                <input asp-for="CouponCode" class="form-control" readonly />
            </div>
            <div class="col-2">
                <label class="control-label pt-2" style="font-size:20px;">Valor do desconto</label>
            </div>
            <div class="col-10 pb-3">
                <input asp-for="DiscountAmount" class="form-control" readonly />
            </div>
            <div class="col-2">
                <label class="control-label pt-2" style="font-size:15px;">Valor mín de aplicação</label>
            </div>
            <div class="col-10 pb-3">
                <input asp-for="MinAmount" class="form-control" readonly />
            </div>
            <div class="col-2">
                <label class="control-label pt-2" style="font-size:20px;">Início da vigência</label>
            </div>
            <div class="col-10 pb-3">
                <input type="datetime-local" asp-for="StartsIn" class="form-control" readonly />
            </div>
            <div class="col-2">
                <label class="control-label pt-2" style="font-size:20px;">Validade</label>
            </div>
            <div class="col-10 pb-3">
                <input type="datetime-local" asp-for="ExpiresIn" class="form-control" readonly />
            </div>

            <div class="col-5 offset-2">

                <a asp-action="CouponIndex" class="btn-primary btn form-control ">Voltar</a>
            </div>
            <div class="col-5">
                <input type="submit" value="Apagar" class="btn btn-danger form-control" />
            </div>
        </div>

        <div>
        </div>
    </div>
</form>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}